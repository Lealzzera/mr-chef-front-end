// import { env } from "@/env";
// import { jwtVerify } from "jose";

// type verifyTokensProps = {
//   accessToken: string
//   refreshToken: string
// }

// export default async function verifyTokens({accessToken, refreshToken}: verifyTokensProps) {
//   if(!token) return false;
//   try {
//     await jwtVerify(token, new TextEncoder().encode(env.JWT_ACCES_TOKEN_SECRET))
//   }
// }
